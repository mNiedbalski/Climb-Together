#pragma once
/*#include "User.h"
#include "Interface.h"
using namespace std;
class Overseer
{
private:
    Overseer() {};
public:
    Overseer(const Overseer&) = delete;
    Overseer& operator = (const Overseer&) = delete;
    Overseer(Overseer&&) = delete;
    Overseer& operator = (Overseer&&) = delete;
    static auto& instance()
    {
        static Overseer overseer;
        return overseer;
    }
    void Process();

};
*/

